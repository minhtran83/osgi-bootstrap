// Top-level build file where you can add configuration options common to all sub-projects/modules.

allprojects { project ->
    buildscript {
        repositories {
            mavenCentral()
        }
    }
    repositories {
        mavenCentral()
    }

    apply plugin: 'java'

}

subprojects {

}

task createWrapper(type: Wrapper) {
    gradleVersion = '1.7'
}


// TODO: clean should remove runner folder; or should it?